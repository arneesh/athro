// import { it, expect } from 'vitest';
// import { bubbleSort, insertionSort, selectionSort, mergeSort, quickSort } from '../src';

// it('sort the array using bubble sort', () => {
//     expect(bubbleSort([1, 4, 2, 6, 3])).toEqual(expect.arrayContaining([1, 2, 3, 4, 6]));
// });

// it('sort the array using insertion sort', () => {
//     expect(insertionSort([1, 4, 2, 6, 3])).toEqual(expect.arrayContaining([1, 2, 3, 4, 6]));
// });

// it('sort the array using selection sort', () => {
//     expect(selectionSort([1, 4, 2, 6, 3])).toEqual(expect.arrayContaining([1, 2, 3, 4, 6]));
// });

// it('sort the array using merge sort', () => {
//     expect(mergeSort([1, 4, 2, 6, 3])).toEqual(expect.arrayContaining([1, 2, 3, 4, 6]));
// });

// it('sort the array using merge sort', () => {
//     expect(quickSort([1, 4, 2, 6, 3])).toEqual(expect.arrayContaining([1, 2, 3, 4, 6]));
// });
